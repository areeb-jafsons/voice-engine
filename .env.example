# =============================================================================
# Abdion Voice Engine — .env.example
# Copy to .env and fill in your real values before running the server.
# NEVER commit your real .env to version control.
# =============================================================================

# ── LiveKit ───────────────────────────────────────────────────────────────────
# Your LiveKit Cloud (or self-hosted) WebSocket URL
LIVEKIT_URL=wss://your-project.livekit.cloud

# LiveKit API key + secret — create in the LiveKit Cloud dashboard
LIVEKIT_API_KEY=APIxxxxxxxxxxxxxxxx
LIVEKIT_API_SECRET=your_livekit_api_secret_here

# ── Deepgram ──────────────────────────────────────────────────────────────────
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# ── Groq ─────────────────────────────────────────────────────────────────────
GROQ_API_KEY=your_groq_api_key_here

# Override the LLM model (optional — defaults to llama-3.3-70b-versatile)
# GROQ_LLM_MODEL=llama-3.3-70b-versatile

# ── ElevenLabs ───────────────────────────────────────────────────────────────
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Voice ID — find at https://elevenlabs.io/voice-library
# Default: Rachel (multilingual, clear diction)
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# ── Server tuning ─────────────────────────────────────────────────────────────
# Maximum simultaneous bot subprocesses (one per call)
MAX_CONCURRENT_CALLS=200

# LiveKit JWT token TTL for bot participants (seconds)
BOT_TOKEN_TTL_SEC=3600

# Seconds to wait for SIGTERM before sending SIGKILL to a bot process
GRACEFUL_STOP_SEC=5.0

# ── Debug ────────────────────────────────────────────────────────────────────
# Set to "true" to enable DEBUG-level logging in both server.py and bot.py
# VOICE_DEBUG=true
